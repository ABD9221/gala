__pycache__/
*.py[cod]
.venv/
venv/
*.duckdb
*.duckdb.wal
# Only the top-level store directory. A bare `data/` also swallowed
# gala/data/, which holds the shipped district catalogs -- reference data the
# package needs, not generated output.
/data/
.cache/
.pytest_cache/
.env

# Generated lead exports. Regenerable with `python scripts/leads.py`, and they
# carry real business phone numbers -- contact data does not belong in git.
leads*.csv
*.leads.csv
